version: "3.9"

services:
  searxng:
    image: docker.io/searxng/searxng:latest
    container_name: searxng
    restart: unless-stopped
    ports:
      - "9009:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:9009/
    volumes:
      - ./searxng:/etc/searxng
